{
  "name": "cvp-web-client",
  "version": "0.0.0",
  "private": true,
  "author": "Cuong Vuong Chi",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint .",
    "dev": "pnpm dev --filter ./packages --parallel",
    "build": "pnpm build --filter ./packages --parallel"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.16.1",
    "eslint": "^8.9.0",
    "eslint-config-prettier": "^8.4.0",
    "prettier": "2.5.1",
    "typescript": "^4.5.5"
  }
}
