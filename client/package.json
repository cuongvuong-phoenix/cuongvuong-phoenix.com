{
  "name": "cvp-web-client",
  "version": "0.0.0",
  "private": true,
  "author": "Cuong Vuong Chi",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint .",
    "dev": "pnpm --filter=./packages --parallel dev",
    "build:ui": "pnpm --filter=ui build",
    "build:website": "pnpm --filter=website build",
    "build": "run-s build:ui build:website"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.16.1",
    "eslint": "^8.10.0",
    "eslint-config-prettier": "^8.5.0",
    "npm-run-all": "^4.1.5",
    "prettier": "2.5.1",
    "typescript": "^4.6.2"
  }
}
